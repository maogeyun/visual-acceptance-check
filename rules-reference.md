# 视觉验收规则参考

**权威来源**：`/Users/hz-mb-0020/Downloads/视觉验收.pdf`（宁波银行 UED《设计还原度量化标准》· 总览 + 视觉要素）

本 skill **仅**覆盖 PDF 两页范围：总览计分规则 + 视觉要素五类检查点。交互/适配/性能类不在本 skill 范围内。

## 定义

**设计还原度**：线上界面与评审确认的设计方案之间的一致程度。

**计分方式**：扣分制。满分 100，最终得分 = 100 − 累计扣分（下限 0）。

## 通用严重度

| 级别 | 代号 | 典型场景 | 默认扣分 |
|------|------|----------|----------|
| 一般 | general | 单参数偏差、次要状态缺失、非关键内容错误 | −1 |
| 较大 | major | 阻断核心流程、缺失关键功能、严重认知混淆 | −2 |
| 致命 | fatal | 完全阻断核心流程、功能整体缺失、严重无障碍违规 | −3 |

各检查点可覆盖上表默认值，以本文件具体条目为准。

## 验收原则（PDF 第 1 页）

**一致性**：视觉风格、交互行为、术语命名、平台规范与设计稿一致。

**清晰度**：内容可读、信息层级明确、布局与留白符合设计系统、颜色对比度满足 WCAG-AA。

## 特殊情形

- **组件技术限制**：经可行性评审确认的技术限制，可标记 `na` 并注明依据，不扣分。
- **跨平台差异**：可控差异扣分；不可控的平台原生差异不扣分，但须满足基线要求。
- **Android 字体降级**：PingFang SC 映射 MiSans/HarmonyOS 时，字重映射与行高补偿按设计系统 Android 规范验收；可控部分按一般问题处理，不可控部分（字重偏差 ≤±100、行高 ≤±2px、字间距 ≤0.5px）不扣分。

## 检查点编码表（报告必引用）

报告「扣分明细」须**逐检查点一行**，使用下表 `checkpoint_code`（前缀 `VA-`）。未检测到的检查点状态为 `untested`，不计分但须列出。

| checkpoint_code | 检查点名称 |
|-----------------|------------|
| VA-COLOR-HEX | 颜色-HEX 匹配 |
| VA-COLOR-BRAND | 颜色-品牌/主题/语义色 |
| VA-COLOR-GRADIENT | 颜色-渐变 |
| VA-COLOR-OPACITY | 颜色-透明度 |
| VA-FONT-FAMILY | 字体-字体家族 |
| VA-FONT-SIZE | 字体-字号 |
| VA-FONT-WEIGHT | 字体-字重 |
| VA-FONT-LINEHEIGHT | 字体-行高 |
| VA-FONT-LETTER-SPACING | 字体-字间距 |
| VA-FONT-DECORATION | 字体-文本变换/装饰 |
| VA-SPACING-BOX | 间距-Padding/Margin/Gap |
| VA-SPACING-GRID | 间距-栅格系统 |
| VA-SPACING-ALIGN | 间距-元素对齐 |
| VA-LAYOUT-MISSING | 布局-模块/元素缺失 |
| VA-RADIUS | 圆角 |
| VA-SHADOW | 阴影-单层参数 |
| VA-SHADOW-MULTI | 阴影-多层 |
| VA-ICON-SIZE | 图标/图像-尺寸 |
| VA-ICON-RATIO | 图标/图像-比例 |
| VA-ICON-RESOLUTION | 图标/图像-分辨率 |
| VA-ICON-SVG | 图标/图像-矢量路径 |
| VA-ICON-CROP | 图标/图像-裁剪对齐 |
| VA-ICON-CLARITY | 图标/图像-轻微清晰度 |
| VA-ICON-FUNCTION | 图标/图像-功能混淆 |

---

## 一、颜色

| checkpoint_code | 检查点 | 验收规则 | 扣分 |
|-----------------|--------|----------|------|
| VA-COLOR-HEX | HEX 匹配 | 色值精确匹配，不接受「接近」 | 任一偏差 −1 |
| VA-COLOR-BRAND | 品牌/主题/语义色 | 与设计系统 Token 一致 | 任一偏差 −1 |
| VA-COLOR-GRADIENT | 渐变 | 起止色、方向、位置完全一致 | 任一偏差 −1 |
| VA-COLOR-OPACITY | 透明度 | 偏差 ≥1% 视为错误 | −1 |

**重复偏差**：同一颜色错误 ≥3 次 → 触发整体等级上限「有条件通过」。

**致命问题**：任一颜色致命问题 → 等级上限「有条件通过」。

---

## 二、字体系统

| checkpoint_code | 检查点 | 验收规则 | 扣分 |
|-----------------|--------|----------|------|
| VA-FONT-FAMILY | 字体家族 | 指定字体，禁止未声明的系统字体回退 | −1/项 |
| VA-FONT-SIZE | 字号 | 零容差 | −1/项 |
| VA-FONT-WEIGHT | 字重 | 零容差 | −1/项 |
| VA-FONT-LINEHEIGHT | 行高 | 零容差 | −1/项 |
| VA-FONT-LETTER-SPACING | 字间距 | 设计有指定则须匹配 | −1/项 |
| VA-FONT-DECORATION | 文本变换/装饰 | 大小写、下划线、删除线一致 | −1/项 |

**叠加**：同一文本块 ≥3 类偏差，额外 −1。

**重复偏差**：同一参数（如字重）在 ≥5 处错误 → 「字体系统性问题」，该根因合计 −7 封顶。

---

## 三、间距与布局

| checkpoint_code | 检查点 | 验收规则 | 扣分 |
|-----------------|--------|----------|------|
| VA-SPACING-BOX | Padding/Margin/Gap | 按 DevTools 盒模型测量 | >1px 偏差 −1/项 |
| VA-SPACING-GRID | 栅格系统 | 严格遵循设计系统栅格 | −1/项 |
| VA-SPACING-ALIGN | 元素对齐 | 无「跳像素」错位 | >1px −1/项 |
| VA-LAYOUT-MISSING | 模块/元素缺失 | 设计稿有而实现无 | 较大 −2，致命 −3 |

**重复偏差**：
- 同一 Spacing Token ≥5 处错误 → 「间距系统性问题」，−7 封顶。
- 不同 Token 间距错误合计 ≥5 处 → 额外 −2（页面级系统性）。

---

## 四、圆角与阴影

| checkpoint_code | 检查点 | 验收规则 | 扣分 |
|-----------------|--------|----------|------|
| VA-RADIUS | 圆角 | 零容差 | −1/参数 |
| VA-SHADOW | 阴影 | X/Y 偏移、模糊、扩展、颜色、透明度五参数全匹配 | −1/参数 |
| VA-SHADOW-MULTI | 多层阴影 | 每一层均须匹配 | −1/层/参数 |

阴影错误影响视觉层级时，单项升至 −2。

**重复偏差**：同一组件 Token 在 ≥5 处错误 → 「组件 Token 问题」，−7 封顶。

---

## 五、图标与图像

| checkpoint_code | 检查点 | 验收规则 | 扣分 |
|-----------------|--------|----------|------|
| VA-ICON-SIZE | 尺寸 | 零容差 | −2 |
| VA-ICON-RATIO | 比例 | 禁止拉伸/压缩 | −2 |
| VA-ICON-RESOLUTION | 分辨率 | @2x/@3x 清晰，高 DPI 不模糊 | −2 |
| VA-ICON-SVG | 矢量图标 | SVG 路径、锚点、填充一致 | −2 |
| VA-ICON-CROP | 图像裁剪 | 对齐与裁剪区域一致 | −2 |
| VA-ICON-CLARITY | 轻微清晰度 | 不影响识别 | −1 |
| VA-ICON-FUNCTION | 功能混淆 | 图标错误导致误操作 | −3 |

**重复偏差**：同一资源同一错误 ≥3 处 → 「资源文件问题」，扣分 = min(单次最大扣分 × 3, 9)。

---

## 质量等级

| 等级 | 分数 | 处置 |
|------|------|------|
| 优秀 | ≥95 | 可上线 |
| 达标 | 90–94 | 可上线；一般问题 1 迭代内修复 |
| 有条件通过 | 80–89 | 较大问题修复后复测；一般问题可延期 |
| 不达标 | <80 | 禁止上线；全面返工复验 |

**一票否决**：
- 出现致命问题 → 整体等级最高「有条件通过」。
- 颜色重复偏差 ≥3 次 → 整体等级最高「有条件通过」。
