# 截图差异精准标注规范

报告必须在**开发页面截图**上对**每一条**差异实例（finding）绘制可见标注。缺 bbox 的 finding 不得进入最终报告。

## bbox 格式（二选一，优先像素）

### 方式 A：像素坐标（推荐，最精准）

```yaml
bbox_px: [left, top, width, height]   # 相对原图左上角，单位 px
image_size: { width: 1440, height: 900 }
```

生成报告时转换为百分比：`left/width*100` 等，保留 **2 位小数**。

### 方式 B：百分比坐标

```yaml
bbox: [left%, top%, width%, height%]   # 相对原图宽高，0–100
```

## 精准度要求

| 规则 | 说明 |
|------|------|
| 元素级框选 | 框住**发生偏差的 UI 元素**，禁止用整页/整区大框敷衍 |
| 误差上限 | 框边与元素可视边界偏差 ≤ **2px**（或 ≤0.15% 视口宽） |
| 缺失模块 | 框住设计稿中该模块应出现的**占位区域** |
| 间距/对齐 | 框住参与间距关系的**两个元素之间**或较小元素本身 |
| 一 finding 一框 | 每条差异恰好 1 个 bbox；多处同类问题拆成多条 finding |

## 标注编号

- `label`：从 **1** 递增的显示编号，与截图标注、差异表 `#` 列一致
- 按页面**从上到下、从左到右**排序后赋号

## 验收自检

生成报告前核对：

```
findings.length === 截图上标注数量
每条 finding 均有 bbox_px 或 bbox
所有 bbox 在图像范围内（0 ≤ x,y；x+w ≤ 100%；y+h ≤ 100%）
```

## 交付物

1. **HTML 报告**（必选）：交互式 SVG 标注，点击表格行高亮对应框
2. **标注截图**（推荐）：用 [scripts/bake-annotations.py](scripts/bake-annotations.py) 烘焙为 PNG，便于 IM/邮件分享
